# RESUME（AI が毎回まず読む・現在地の軽量ファイル）

> このファイルの役目：**セッション開始時に AI がこれだけ読めば再開できる**現在地メモ。短く保つ。
> 詳しい全履歴・学習設計の根拠・過去の事故・教材取得の詳細・フック仕様は `STUDY_LOG.md`（重い・人間の「できた日記」）にある。**必要になったときだけ STUDY_LOG を開く。**
> 読む順：**RESUME.md →（この節を守る）→ CLAUDE.md（役割と2原則）**。STUDY_LOG は原則読まない。

---

## 🟡 最重要・温度（進度より優先。矛盾したらこちらを採る）

本人が「つらい記憶ばかり積み上がって数学が億劫」と吐露した所からの運用。詳細は STUDY_LOG「🟡🟡 2026-07-14 最重要・情緒」。

1. **1セッション＝できること1つだけ。できたら即・気持ちよく終える。**「もう1問」を AI から言わない。進度を焦らない。
2. **記録は「今日できたこと」だけ（できた日記）。詰まりの原因分析・AI 反省を書かない。**
3. **原因追求は封印。** 本人が「なんで?」と聞いたときだけ答える。「なぜできなかったか」を AI から掘らない。
4. **本人に決定・選択を求めない**（言質不安の元）。進め方は AI が静かに引き受けて決め、嫌なら言ってもらう形。AskUserQuestion の多択で迫らない。
- 入り方は **絵のある所・1回で腑に落ちる小さな1つ**から。ドリルや「通しでやり直し」の負荷から始めない。
- 冒頭のウォームアップ（間隔反復）は**軽く・できなくてもすぐ流す**。プレッシャーにしない。
- 「気が変わってOK・前と違うことを言ってOK・ログは縛る鎖でなく忘れていいための外部メモ」を折に触れ伝える。

---

## 現在地（2026-07-15 時点）

- **ch03「合成関数の微分」＝全節クリア済（s6「対数微分」まで完了）。ch01・ch02 も全節クリア済。**
- s6 の到達点：例題3-6-1（x^x）／例題3-6-2（(x+1)²(x+3)³）／問題3-6-3（√(x+2)(x+1)³）をいずれも対数微分で自力完走。log(ab)=log a+log b、log(a^b)=b log a は定義から自力証明済。
- 問題3-6-3（2026-07-15）：骨組みはゼロから自力構築。唯一詰まったのは (√(x+2))′ を −(x+2)⁻² と置いた所→「√=1/2乗」に自分で戻して回復。整理形は任意・未処理でOK。対話 `03-chain-rule/dialogue-06-log-diff-3-6-3.md`。
- **次にやるなら**（本人が数学に戻りたくなったら・進度は二の次）：**ch04 に入る**。まだ教材未取得＝入る時に fetch（実務メモ参照）。🟡 の温度のまま・焦らない。
- 教材は `03-chain-rule/material.md` に ch03 全節取得済み。ch04 以降は未取得。

---

## 実務メモ（最小限）

- **チャットの数式は Unicode プレーン表記**（e^p, x², f′(x), log x）。**インライン `$...$` はチャットで禁止**（端末で生表示され読めない）。強調する独立式だけ `$$...$$` ブロック。※ .md ファイル本文は `$...$` でOK。
- **教材取得**（必要時のみ）：`tools/fetch.sh "<references URL>"`。`[]` が返ったら `tools/zen-login.sh` で再ログイン。URL・movieId は STUDY_LOG「URL構造のメモ」。
- 図は uv プロジェクトで生成（`uv run python ...`、`scratchpad/plot_setup.py`）。作った図は `<章>/images/` に PNG＋再生成スクリプトで残す。
- 決定論的な強制はフック任せ（`~/.claude/hooks/`）。フックは命綱だが完璧でない＝運用（短く切る・1節ごと /clear）が本体。
